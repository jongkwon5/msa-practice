version: '3.8'

services:
  jenkins:
    build: .
    container_name: jenkins
    ports:
      - "28080:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - ../:/home/msa-practice
      - /var/run/docker.sock:/var/run/docker.sock  # <- 추가

volumes:
  jenkins_home: